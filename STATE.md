# STATE.md — cross-session memory
> Protocol: `.claude/skills/project-memory`. Read this FIRST every session. Update it before ending any session that did work. Durable info only — git history is the journal, plans/ is the tracker.

## Current
- **Programme**: Shopify-parity, planned in `plans/` (master: `plans/000-master-plan.md`). **P1 done + LIVE on prod (`main`=b1e9893).**
- **Active phase**: **P2 — AUTONOMOUS RUN P2→P6** (user directive 2026-06-13: "do each phase, involve me only on the final push after P6; use workflows/loops/subagents/adversarial agents; spawn as many agents as needed"). See `## Autonomous run` below for the operating contract. All work on `sprint-abe`; no main merges, no prod-applies until the post-P6 push.
- **Branch reality**: `main` = `b1e9893` (P1 live). `development` = `sprint-abe` diverge as P2+ commits land on sprint-abe; periodic sprint-abe→development PRs for CI only.
- **Next concrete action**: P2-00/01/02/04/10/11 DONE (P2-04 7307d4e — repaired: calculateOrderTotals now the single charge source, both routes wired, flag-OFF mutation-proven byte-identical; P2-04a client-server-totals deferred). **P2 CODE COMPLETE** — all packets done (P2-08 d07da87). Deferred P2 follow-ups (non-blocking): P2-02a (schema-DRIVEN full SchemaForm — prereq before P3-05/P4-02), P2-04a (checkout client server-totals), P2-08a (dead payload scripts), P2-09a (app/api/chat console→logger). #G-P2 = USER GATE (evidence batched; NOT auto-passed). **Now: P3 ∥ P4** — master plan runs them parallel, but I serialize ONE code packet per wave (test-surface conflict). P3-01 DONE (fe12352 — content schema + content-store + reserved-slugs; drizzle/0006 DO-block enums/FK, parse-validated; pg-query-emscripten added as the standard migration-parse-validation devDep). P3-09 DONE (87454c4). P3-10 DONE (1f04bf6 — homepage→blocks flag-gated FTT_FEATURE_BLOCKS_HOMEPAGE [OFF=hardcoded byte-identical, ON=real renderBlock engine, load-bearing proven 2 ways]; equivalence test; build-not-run seed; 1150 tests; ACCEPT-WITH-MINORS → P3-10a parity-gaps[TrustSignals/HowItWorks unmapped]+converse-equivalence, P3-10b[BATCHED #G-P3] switch flag-on homepage to read DB not fixture before prod flip). **P3 COMPLETE (P3-00→10).** Now: **P5 — Channels & Control Centre.** Autonomous P5 packets (build+fixture-test): P5-00 spike[doc], P5-01 Google Merchant feed, P5-02 Meta feed[shared lib/channels/feed-mapping.ts], P5-04 pull-adapters[channel-metrics ports + search-console/ga4/vercel/meta adapters fixture-tested + cron→channel_metrics], P5-05 Control Centre admin, P5-06 AEO[llms.txt/FAQPage/Product-schema-test/OG-images], P5-07 reservation-expiry email[dedupe col migration build-not-run]. **BATCHED: P5-03 console wiring (creds+consoles) + #G-P5.** P5-01 DONE (4fb5623). P5-02 DONE (f002c55 — Meta catalog CSV /api/v2/feeds/meta-catalog.csv REUSING lib/channels/feed-mapping.ts [mutation-proven no-drift: changing the shared mapping fails BOTH feeds]; RFC-4180 parsed tests; Google feed unregressed [feed-mapping byte-identical]; 1225 tests; opus 3-lens ACCEPT → P5-02a reuse-test-absolutes, P5-02b additional_image_link-comma-list). Next concrete action: **P5-04 pull adapters** (lib/ports/channel-metrics.ts + 4 read-only adapters search-console/ga4-data/vercel-insights/meta-marketing, each typed+error-isolated+unit-tested vs FIXTURE responses [NO live calls]; cron /api/v2/cron/refresh-channel-metrics [CRON_SECRET pattern] caches into a new channel_metrics table [migration build-not-run]; per A6 adapters fall back to empty/dev when creds absent — prod creds BATCHED). Then P5-05 Control Centre / P5-06 AEO / P5-07 reservation-email. **P5-03 console wiring BATCHED.** Then P6. **Done so far: P3-00→10, P4-00→06, P5-00/01/02, P2-02a; 1225 tests.** **P4-07 (retire legacy details* columns) is BATCHED** — depends P4-02 stable IN PROD + the P4-01 backfill migration (prod-gated; do at #G-P4, not autonomously). Then P5, P6. **Done so far: P3-00/01/02/03/04/05/06, P4-00/01/02/03/04/05/06, P2-02a; 889 tests.** NOTE: `.env.production.example` PARKED in `git stash@{0}` (restore at final push, BATCHED). Ship pipeline: INLINE workflow scripts only (scriptPath+args unreliable); opus reviewers (fable alias unavailable); WORKER TEST DISCIPLINE — mutation-proofs must test the REAL unit, mocking only the lowest dep (mock @/db not @/db/queries/*; mock next/headers not the guard; collectPrimitives for WHERE) — recurring REJECT cause. **Discipline: ONE code packet/wave; PLAIN-JS workflows; EXPLICIT-pathspec commits; PARSE-VALIDATE migrations (build-not-run); EXCLUDE .env.production.example + untracked labs/xeno/onboarding.**

## Autonomous run (P2→P6) — operating contract
Loop: each iteration read STATE + active plan → run next ready packet(s) through ship pipeline via Workflow → clean-worktree verify (tsc+tests) → adversarial review → commit scoped paths to sprint-abe → check plan box + update STATE → continue. 2 failed verify loops → mark packet BLOCKED + record, continue others. Driven by workflow-completion re-invocation + a long fallback wakeup. Stop & notify the user only when all P2–P6 CODE packets are done (gates/prod-applies remain) or a true hard blocker halts all progress.
**Assumptions made to avoid blocking (confirm at final push):** A1 #G-DOMAIN→canonical `www.fromthetrunk.shop`, .com unwired. A2 P4 types→preloved-saree(backfill)+blouse+accessory, one-of-one, no variants. A3 P5 feeds/adapters built+fixture-tested; console submission+credentials batched. A4 prod migrations built+unit-tested; Neon rehearsal+prod-apply batched. A5 all #G- checkpoints→evidence prepared, batched. A6 external svcs (Upstash P2-06, Sentry P6-07) behind ports w/ in-memory/dev fallback; prod creds batched. A7 no main merges.

## BATCHED FOR USER (the final-push review — populate as the run proceeds)
- #G-DOMAIN decision (.shop confirmed canonical? wire .com?).
- **P2-04 GST value question**: reinterpreting pricePaise as GST-inclusive + removing the checkout +12% means either MIGRATE existing prices ×1.12 (preserve all-in amounts customers pay) OR effective prices drop ~12%. Business/data decision required before flipping `FTT_FEATURE_GST_INCLUSIVE` on in prod (the code ships flag-off, behavior-preserving).
- Confirm P4 product-type taxonomy (assumed saree+blouse+accessory).
- P5 external credentials + console submission (GMC, GSC, GA4, Meta, Vercel API token) + GTIN-exemption.
- Prod migration sign-offs: **P2-05 inventory v2 (drizzle/0004_inventory_v2.sql built, IF-NOT-EXISTS, NOT run — needs Neon rehearsal of the backfill rowcounts then prod migrate; then flip FTT_FEATURE_INVENTORY_V2)**, P4-01 backfill, P4-07 column drop. **Built-not-run migrations 0004–0010** (0004 inventory, 0005 events, 0006 content, 0007 product_types, 0008 collections, 0009 tags, 0010 theme_versions) all need Neon rehearsal + prod-apply BATCHED, AND **drizzle-meta/_journal.json reconciliation** (0010 — and verify 0004–0009 — are registered in the journal; + the out-of-band 0002_agent_panel drift) before any drizzle-kit migrate.
- **#G-P2 evidence (P2 code done 2026-06-13)**: (a) schema-form renderer wired into product stepper details, 15 anys killed — full schema-DRIVEN demo pending P2-02a; (b) inventory-v2 migration drizzle/0004 built (IF-NOT-EXISTS) but NOT run — needs Neon-branch rehearsal (backfill rowcounts: available→1/sold→0/reserved→1+reservation row) + rollback=restore-branch before prod migrate; (c) GST-inclusive pricing implemented behind FTT_FEATURE_GST_INCLUSIVE (off) — flip needs the ×1.12 value decision; (d) CI green WITHOUT --legacy-peer-deps (Payload excised, undici direct, -304 pkgs/-263M, build 31/31). Decide: approve prod migration + flag flips, or adjust.
- #G-P3/#G-P4/#G-P5/#G-P6 user checkpoints (evidence prepared per phase).
- Final consolidation `development → main` + prod deploy(s) for P2–P6.
- P1 leftovers: P1-15 (unpublish "test chiffon"), Xeno relocation+redaction, admin-orders-LIST rewrite (in `git stash` "P1-20 deferred").

## Standing facts (verified 2026-06-13)
- Tests 174/174 pass; tsc clean; lint clean.
- Canonical prod domain is `https://www.fromthetrunk.shop`; `fromthetrunk.com` does NOT resolve yet is the code fallback (P1-14). Prod also serves at ftt-fromthetrunk.vercel.app.
- `CRON_SECRET` is configured in prod (release-reservations cron probe returns 401, not 500).
- Production has ZERO analytics (verified in repo and live HTML).
- pricePaise is GST-EXCLUSIVE; checkout adds 12% (`lib/payments/razorpay.ts:182`). Feed work gated on #G-GST (now resolved — P2-04 will make pricePaise GST-inclusive).
- Prod data contains a published test product ("test chiffon do not buy if not authorized") — P1-15.

## Known failure modes (traps for future sessions)
- **`formatINR` exists twice with different units** — db/money.ts takes PAISE, lib/email/templates.ts:44 takes RUPEES. 100× display bug if confused (P1-10 pending).
- **`git add .` is catastrophic here** — ~735MB untracked (P1-01 added .gitignore guards but dirs still exist). Always add scoped paths.
- **Extraction-pipeline JSON-LD false positive**: the site's ld+json is valid; naive scrapers that unescape `\n` reproduce a phantom "control character at 457" parse error. Verify the served artifact, not a re-processed copy.
- **Hot-deploying from unmerged branches loses fixes**: the May guest-checkout hotfix was silently overwritten by the next main deploy. PR-only promotion exists (`enforce-pr-only.yml`) — don't bypass it.
- **Drizzle mock AST inspection for WHERE predicates**: `whereMock.mock.calls` arguments use `arg._and._ne` shape. Removing the predicate leaves tests green if not asserted — always add the AST filter.
- **P1-05a TOCTOU**: two concurrent admin PATCH tabs both pass `isStatusChange`, both email — deferred sub-item.
- **Drizzle migration journal drift**: `drizzle/0002_agent_panel.sql` was applied to the live DB out-of-band and is absent from `drizzle/meta/_journal.json`. Future `drizzle-kit generate` will re-emit those DDL statements — always use `IF NOT EXISTS` for chat_conversations columns in new migrations until the journal is reconciled.
- **JSON-LD `</script>` not escaped**: `JSON.stringify` does not escape `<` — embedded `</script>` in product fields creates a potential DOM injection in the page's ld+json script tag. Fix tracked separately (spawned task).
- **orders.userId is now nullable** (as of 7fdb2f9): any code that previously assumed `order.userId: string` may need null guards. Guest orders have userId=null; access via verifyOrderAccessToken.
- **Postgres `IF NOT EXISTS` is NOT valid on `CREATE TYPE` or `ADD CONSTRAINT`** (found at P3-01): only TABLE/COLUMN/INDEX support it; `CREATE TYPE IF NOT EXISTS` / `ALTER TABLE ADD CONSTRAINT IF NOT EXISTS` are syntax errors ("at or near NOT") that abort the whole migration → zero objects created. For idempotent enums/constraints use `DO $$ BEGIN <stmt>; EXCEPTION WHEN duplicate_object THEN null; END $$;`. **Migration verifiers MUST parse-validate the .sql against real PG grammar (pg-query-emscripten/libpg_query), NOT grep-count IF-NOT-EXISTS tokens** (the verifier passed it; adversarial review caught it). 0004/0005 are clean (table/column/index only); 0006 had the bug (repaired).
- **Removing a package can drop a TRANSITIVE dep that committed code directly imports** (found at P2-08): `db/index.ts` directly `import`s `undici`, which was only present transitively via payload. Excising payload → node_modules/undici gone → tsc/tests/build all red (can't resolve undici). Fix = add undici as a DIRECT dep (it always should have been). Lesson: before removing a big dep, grep committed code for imports of packages the dep supplied transitively, not just imports OF the removed package. Adversarial review (real `npm run build`) caught the RED build; the worker mislabeled it "pre-existing".
- **Parallel opus mutation-probes can CORRUPT untracked working-tree files** — the 3-lens panel runs concurrently and each reviewer mutates source (mutate→test→revert) to verify mutation-proofs; on P3-09 a reviewer's perl/python edit corrupted the UNTRACKED `navigation.ts` (the harness can't `git checkout`-restore untracked files), and 3 concurrent reverts race. The reviewer hand-reconstructed it. MITIGATION: after ANY repaired/ACCEPTED packet, the orchestrator MUST independently re-run `tsc --noEmit` + `npm test` (+ grep the key invariants) on the working tree BEFORE committing — do not trust the panel's "restored byte-identical" claim. (Discovered P3-09.)
- **Reusable Workflow `scriptPath` + `args` INTERMITTENTLY drops the args payload** — a generic ship script that reads `args` ran fine for one packet (P3-05) then got an empty `args` the next (P3-06), so the worker built NOTHING and the panel re-reviewed the previously-committed packet (a silent full-pipeline no-op; tell-tale: result.packetId is the default). MITIGATION: use INLINE workflow scripts (embed the packet text directly in `script`) — reliable across P4-05 ×2 + P3-06. Don't trust scriptPath+args for the packet spec. (Discovered P3-06.)
- **MOCK THE DRIVER, NOT THE UNIT UNDER TEST** (recurring REJECT — P4-04 theater, P3-06 guard, P4-06 bulk helpers) — the worker (sonnet) habitually writes tests that mock one level too high: `vi.mock("@/db/queries/*")` to test a route (proves only arg-forwarding, never the WHERE/IN clause), or testing the RSC instead of the guard. A test that mocks the function under test is THEATER and a mutation in that function stays green. RULE for every packet: mutation-proofs test the REAL unit with only the LOWEST dep mocked — mock `@/db` (the drizzle builder) not `@/db/queries/*` and assert the captured WHERE with the collectPrimitives WeakSet AST walker (examples: tests/unit/postgres-catalog-search.test.ts ~:102, payments-cap.test.ts ~:73, webhooks-route.test.ts ~:71); mock `next/headers` not the guard. Bake this into the worker prompt + verifier checklist of every packet.
- **Mutation-prove security/data-integrity guards by EXTRACTING them into pure functions** — a load-bearing guard left inline inside an async RSC / next/headers boundary (P3-06: the no-draft-leak check in app/(site)/[...slug]/page.tsx) is effectively untestable, so removing it passed the entire suite (the verifier can't catch a coverage gap; only an adversary mutating the guard + watching green does). Fix pattern: pull the invariant into a pure exported helper (e.g. shouldRenderDraft()) the boundary calls, and mutation-prove the helper. (Discovered P3-06.)
- **`model:'fable'` (claude-fable-5) is UNAVAILABLE inside Workflow subagents this session** — the adversarial reviewer call throws "model may not exist / no access". Per user directive, use `model:'opus'` (opus 4.8) for fable-reviewer agents in all ship workflows. (Discovered P4-05.)
- **Diff-baseline pollution trips scope/verifier checks** — verifiers & reviewers reason from `git diff HEAD`, but this working tree permanently carries pre-existing uncommitted batched files (notably the TRACKED `.env.production.example` with P2 flag docs; also untracked labs/xeno/onboarding). They show up as "out-of-scope file modified" FALSE FAILs. Mitigation: PARK polluting tracked files with `git stash push -- <path>` before a packet's review/commit (it's in `git stash@{0}` now), AND give reviewer prompts an explicit IN-SCOPE file list with "ignore other dirty files". (Discovered P4-05.)
- **RECURRING: workers build correct new logic but leave the real consumer path on the OLD implementation** (P2-04: calculateOrderTotals had zero callers, charge math was inline-duplicated in payments.ts/orders.ts; P4-03: getCollectionProductIds union resolver was dead, public page + product-grid used legacy products.collectionId). STANDING MITIGATION baked into specs: every packet that adds/refactors a "central" function MUST grep its call sites and WIRE + test the ACTUAL render/charge/consumer path; verifier must assert it is not dead code (grep a real call site); adversarial review reliably catches this, plain verifier often does not.

## Decisions
- **2026-06-12**: No replatform to Shopify — review evidence shows the custom core out-models Shopify for one-of-one inventory; gap is operational features → phased plan in plans/. (Full rationale: plans/000-master-plan.md §7.)
- **2026-06-12**: Small-model execution via packet pipeline (repo-scout → implementation-worker → verifier → fable-reviewer); maker never grades itself; 2-loop escalation to Fable-class re-spec.
- **2026-06-12**: Kept kit agents (scout/worker/verifier names referenced by workstream skills); added repo-scout/implementation-worker/fable-reviewer as ship-pipeline specialists; verifier upgraded in place with the ladder.
- **2026-06-13**: #G-GST resolved — GST-inclusive prices going forward ("for now" — revisit if pricing model changes). pricePaise will be redefined as GST-inclusive in P2-04; `razorpay.ts:182` add-on removed. P2-03 gate closed. Unblocks P5 feed work (Google India requires GST-inclusive prices matching landing page).

## Log
### 2026-06-14 — P5-02 Meta catalog feed (inline ship pipeline; opus 3-lens; ACCEPT no REJECT)
- **Changed**: api/hono/routes/feeds.ts (+147: buildMetaCatalogCsv + /meta-catalog.csv route) + tests/unit/meta-catalog-feed.test.ts. Commit f002c55.
- **Verified**: tsc 0; 1225 tests (+33). No-drift mutation-proven: changing lib/channels/feed-mapping.ts price/availability fails BOTH Google + Meta tests. Meta CSV RFC-4180 parsed (not string-matched); exclusions mutation-proven ABSENT; token gate all branches; Content-Type text/csv. Google feed unregressed (feed-mapping.ts byte-identical to HEAD; google suite 42/42). I re-verified the tree myself (no /1000 residue, feed-mapping diff empty, 1225 green) after TWO lenses reported corrupting+restoring feeds.ts during parallel mutation-probes (the recurring untracked-file race).
- **Decisions**: Meta serializer is pure formatting over the shared FeedItem — only the CSV column names/value formats differ from Google's RSS. additional_image_link emits the first additional image only (Meta accepts; P5-02b for full comma-list parity). No reimplementation = feeds provably can't drift.
- **Next concrete action**: P5-04 pull adapters (channel-metrics port + 4 fixture-tested adapters + cron → channel_metrics table, build-not-run migration).

### 2026-06-14 — P5-00 channel-audit spike + P5-01 Google Merchant feed (inline ship pipeline; opus 3-lens; ACCEPT no REJECT)
- **Changed**: docs/spikes/channel-audit.md (P5-00 orchestrator doc); lib/channels/feed-mapping.ts + api/hono/routes/feeds.ts + api/hono/app.ts (mount) + db/queries/reservations.ts (getBatchActiveReservationsCounts) + tests/unit/google-merchant-feed.test.ts (P5-01). Commits 8b4fa61 (spike box) + 4fb5623 (P5-01).
- **Verified**: tsc 0; 1192 tests (+42). Feed served at /api/v2/feeds/google-merchant.xml (proven via app.fetch HTTP tests → 200/403/application/xml). Price=pricePaise/100 mirrors json-ld (GST backed out, not added → feed===PDP, mutation-proven). Availability via REAL deriveStockStatus + batched reservations (expired-reservation→in_stock proven through the live route). Exclusions mutation-proven ABSENT on PARSED XML. Test discipline honored (mock @/db only; mapping/serializer/listProducts run for real) — NO test-theater REJECT (2nd packet running clean with the directive baked in).
- **Decisions**: feed reuses the sitemap's exact listProducts query (can't drift). Shared mapping module created in P5-01 (not deferred to P5-02) so both feeds share one source. flag-agnostic price at the mapping layer is CORRECT (GST inclusive mode backs tax out for display). Console submission + GTIN-exemption ops = BATCHED (P5-03).
- **Next concrete action**: P5-02 Meta catalog feed reusing lib/channels/feed-mapping.ts.

### 2026-06-14 — P3-10 migrate homepage to blocks + P3 COMPLETE (inline ship pipeline; opus 3-lens; ACCEPT-WITH-MINORS)
- **Changed**: app/(site)/page.tsx (flag-gated render), lib/config/flags.ts (isBlocksHomepage), lib/content/seed/homepage-blocks.ts (fixture), scripts/seed-homepage-cms.ts (build-not-run seed), .env.example, tests/unit/homepage-blocks.test.ts. Commit 1f04bf6.
- **Verified**: tsc 0; 1150 tests (+28). I INDEPENDENTLY re-ran tsc + full suite + grepped the invariant (page.tsx:35 = if(isBlocksHomepage()), not if(true); renderBlock wired) after the parallel panel probes — green before commit. Flag-on renders via the REAL renderBlock engine (proven 2 ways: flag-off if(true) mutation + nulling renderBlock each break a test). Flag-off byte-identical. Seed not run.
- **Decisions**: homepage sections mapped to existing blocks (hero/story-editorial/product-grid source=featured/newsletter-signup); TrustSignals + HowItWorks have no block → documented acceptable deltas (P3-10a). Flag-on reads the in-code fixture (no DB dep) for the POC; switching it to read the DB version is required before the prod flag flip (P3-10b, batched #G-P3).
- **P3 PHASE COMPLETE**: P3-00 spike, P3-01 schema, P3-02 registry+3 blocks, P3-03 public renderer, P3-04 pages admin, P3-05 page editor, P3-06 preview/publish, P3-07 theme, P3-08 6 blocks, P3-09 nav/redirects, P3-10 homepage migration. The no-code content engine is end-to-end: compose (P3-05) → preview/publish (P3-06) → render (P3-03) with theming (P3-07), all blocks (P3-02/08), nav/redirects (P3-09). #G-P3 (editor usability test) is BATCHED.
- **Next concrete action**: P5 — start with P5-00 spike (channel-audit doc) then P5-01 Google Merchant feed via inline ship pipeline.

### 2026-06-14 — P3-09 navigation & redirects managers (inline ship pipeline; opus 3-lens; REJECT→repair→ACCEPT)
- **Changed**: lib/content/nav-menu.ts + redirect-resolver.ts (NEW), api/hono/routes/navigation.ts + redirects.ts (NEW) + app.ts mount, components/layout/{nav-data,site-header-server,site-footer-server}.tsx (NEW) + site-header/footer.tsx (consume) + app/(site)/layout.tsx, proxy.ts (redirect consult + checkout exclusion), db/queries/content.ts + content-store port/adapter (menu/redirect), app/(admin)/admin/{navigation,redirects}/page.tsx (NEW) + nav-items.ts, 5 tests. Commit 87454c4.
- **Verified**: tsc 0; 1122 tests (+60). I INDEPENDENTLY re-ran tsc + full suite after the panel (because a reviewer corrupted+reconstructed untracked navigation.ts during mutation-probing) — confirmed green + invariants present before commit. Loop guard mutation-proven (cycle ≤3 DB calls, doesn't hang); footer round-trip proven through REAL route+store; toPath open-redirect mutation-proven closed; checkout /cart excluded from redirects.
- **Decisions**: 1st opus-panel round REJECTed (2/3): the FOOTER menu could never save — admin posted FooterSection[] but the route validated ONE flat {label,href}[] schema for both slots → 400 on every footer save (the #1 dead-consumer-path class), hidden by a mislabeled "round-trip" test that injected sections straight into the mocked @/db (test theater). Repair branched the footer POST schema (sections + isValidHref on nested hrefs) + a REAL route+store round-trip test + closed the toPath open-redirect (admin-gated but a real surface). Deferred the broadened-proxy per-request redirect DB query to P3-09a (perf, not correctness).
- **New failure mode**: parallel mutation-probes corrupting untracked files → orchestrator must re-verify tsc+tests before commit (see Known failure modes).
- **Next concrete action**: P3-10 migrate one real page (LAST P3 packet) via inline ship pipeline.

### 2026-06-14 — P3-08 remaining v1 blocks (inline ship pipeline; opus 3-lens; REJECT→repair+decision→ACCEPT)
- **Changed**: 6 new lib/content/blocks/* (announcement-bar, faq, image-text-split, newsletter-signup, spacer, story-editorial) + registry.ts + block-editor-schemas.ts; FT-12 list-of-text FieldType (lib/forms/types.ts + components/admin/schema-form/schema-form-field.tsx + lib/catalog/type-schema.ts exhaustiveness); components/sections/newsletter.tsx (optional props); tests block-faq-json-ld + block-p308-new-blocks + adopted forms-types.test.ts. Commit 169e45b.
- **Verified**: tsc 0; 1062 tests (+143); lint clean; no migration. FAQ JSON-LD rendered-output mutation-proven (real renderBlock, parse actual ld+json, </script> escaped). Newsletter one-h3 mutation-proven (reverting fix fails). FT-12 render-test mutation-proven (emitting objects fails). announcement-bar non-dismissible (matches block-inventory).
- **Decisions**: 1st opus-panel round REJECTed (2/3): (a) newsletter double-heading [block rendered editable copy ABOVE <Newsletter> which also hardcoded its own → two stacked headings] — REAL bug, repaired (made editor copy authoritative via optional NewsletterProps + fallback). (b) announcement-bar "dismissible" — a SPEC CONFLICT (packet wording vs block-inventory BLOCK-08 which specs no dismiss); reviewers correctly escalated to principal → I RESOLVED it: block-inventory is ground truth, non-dismissible v1 accepted, dismiss → P3-08b. New FT-12 FieldType was needed for FAQ items/story beats — worker REPORTED it per the "report don't silently add" rule (correct). Adopted the long-pending untracked forms-types.test.ts (P2-01a forms-types half done).
- **Next concrete action**: P3-09 navigation & redirects managers via inline ship pipeline.

### 2026-06-14 — P3-07 theme tokens + editor (inline ship pipeline; opus 3-lens; ACCEPT-WITH-MINORS)
- **Changed**: lib/content/theme-tokens.ts + theme-settings.schema.ts (NEW token schema), components/layout/theme-styler.tsx (NEW RSC injection), app/(site)/layout.tsx (render ThemeStyler), api/hono/routes/theme.ts (NEW) + api/hono/app.ts (mount), app/(admin)/admin/theme/page.tsx (NEW editor) + components/admin/nav-items.ts, lib/ports/content-store.ts + lib/adapters/drizzle-content-store.ts + db/queries/content.ts (theme get/upsert/version), db/schema.ts (theme_versions), drizzle/0010_theme-versions.sql (NEW, not run), 3 tests. Commit bef89db.
- **Verified**: tsc 0; 919 tests (+30); lint clean; 0010 pg-query parse-valid. Injection mutation-proven vs the REAL emitted :root __html (theme-styler-consumer.test.ts — real RSC, only store mocked); persistence mutation-proven via REAL query (theme-route.test.ts mocks @/db not the query module + collectPrimitives). No-theme default byte-identical. Test-discipline directive worked — worker used the real-unit/low-mock pattern, NO test-theater REJECT this time (1st packet with the directive baked in).
- **Decisions**: theme overrides EXTEND the Tailwind v4 contract via the @theme inline var() indirection (override --primary etc. → propagates to utilities); does not fork docs/design-system.md. Versioning mirrors page_versions (immutable theme_versions rows). Avoided adding a color-picker FieldType (used text + hex validation) — no lib/forms scope creep. verifyLoops 1 + reviewLoops 1 (worker initially shipped fontPair/CURATED_FONT_PAIRS cruft + a journal/validation nit; repaired).
- **Next concrete action**: P3-08 remaining v1 blocks via inline ship pipeline.

### 2026-06-14 — P4-06 bulk operations (inline ship pipeline; opus 3-lens; REJECT→repair→ACCEPT)
- **Changed**: api/hono/routes/admin-import.ts (type-aware import + attr validation), api/hono/routes/products.ts (/bulk-edit + /export.csv routes + pipe-join export), api/hono/schemas/products.ts, app/(admin)/admin/products/page.tsx (multi-select + bulk-action bar), components/admin/import/step-map.tsx, lib/import/field-mapper.ts, db/queries/products.ts (updateProductsBatch/bulkSetProductTags), db/queries/collections.ts (bulkAdd/RemoveProductsToCollection), + 7 tests incl. bulk-edit-db-queries.test.ts. Commit f659974.
- **Verified**: tsc 0; 889 tests; lint clean; no migration. Type-aware import mutation-proven (neuter buildTypeZodSchema → fail). Bulk-edit DB helpers DB-layer mutation-proven via collectPrimitives (mock @/db, REAL helpers): single-row WHERE / dropped collectionId guard / wrong tag-merge each go RED (all 3 lenses reproduced). Multi-select export pipe-joined to round-trip with import split (comma-join → RED).
- **Decisions**: 1st opus-panel round REJECTed (2/3): the bulk-edit DB helpers were 100% mocked at the @/db/queries/* boundary → a single-row WHERE bug left all 879 tests green (test theater the verifier passed). Also a real bug: multi-select CSV export didn't pipe-join arrays → broke round-trip with import. Targeted repair: collectPrimitives DB-layer tests (mock @/db not the query module) + the export join fix. Import path now also enforces the type schema (partial P4-02a hardening; the product create/update API route still needs it server-side — P4-02a stays open). status enum is draft/published (packet wrongly said active/archived; worker used the real enum — correct).
- **New failure mode**: **mocking at the MODULE boundary (vi.mock @/db/queries/*) hides bugs in the query logic** — the route test only proves arg-forwarding; the WHERE/IN clause is never exercised. Mock ONE level lower (the @/db builder) + walk the captured WHERE with collectPrimitives. Generalized into worker test-discipline (see Known failure modes). This is the SAME class as P3-06 (guard inside an untestable boundary) — both fixed by testing the real unit with only the lowest dep mocked.
- **Next concrete action**: P3-07 theme tokens + editor via inline ship pipeline.

### 2026-06-14 — P3-06 preview + publish pipeline (inline ship pipeline; opus 3-lens; REJECT→repair→ACCEPT)
- **Changed**: lib/content/preview-token.ts (NEW — HMAC token + shouldRenderDraft gate), app/api/preview/route.ts (NEW), app/(site)/[...slug]/page.tsx (draftMode + shouldRenderDraft), lib/ports/content-store.ts + lib/adapters/drizzle-content-store.ts + db/queries/content.ts (publishPage/unpublishPage), lib/ports/analytics-sink.ts (content_published), api/hono/routes/pages.ts (publish/unpublish/preview-token endpoints), app/(admin)/admin/pages/page.tsx + [id]/edit/page.tsx (controls), .env.example (CMS_PREVIEW_SECRET), tests preview-guard/preview-route/preview-token/publish-pipeline. Commit a7bbcb3.
- **Verified**: tsc 0; 845 tests (+9); lint clean; no migration. Guard MUTATION-PROVEN: dropping `&& verifyPreviewToken` from shouldRenderDraft reddens 6/9 preview-guard cases (no-token/forged×3/cross-slug×2) + the full suite (verifier + all 3 opus lenses reproduced it independently). Publish/unpublish/rollback flip published_version_id via content-store port; revalidateTag('page:'+slug) asserted; content_published fire-and-forget (throwing sink doesn't fail publish).
- **Decisions**: 1st opus-panel round REJECTed (2/3 lenses) — the load-bearing no-draft-leak guard lived inline in the async RSC app/(site)/[...slug]/page.tsx and had ZERO test coverage; removing the token check passed all 836 tests. Cross-slug replay is the real vector (Next draftMode cookie is NOT slug-bound, so the per-page token check is the only slug-rebind). FIX = extract the gate to a pure exported shouldRenderDraft() and mutation-prove it (the in-workflow repair loop had tried option B — testing the RSC directly — and failed; a targeted repair with option A = pure-function extraction succeeded). Rollback reconciled with P3-04 restore (one shared publishPage path). P3-05b (autosave version-spam) still open — preview/publish didn't change the append-per-change draft model.
- **New failure modes**: (1) **Reusable Workflow scriptPath+args silently DROPPED its args payload once** (P3-06 first run wq=w1ihbxwof): worker got no packet → built nothing → reviewers re-reviewed the prior committed packet; result.packetId='packet' (default). It worked for P3-05 then failed for P3-06 — INTERMITTENT/unreliable. MITIGATION: use INLINE workflow scripts (embed the packet in `script`) — reliable (worked for P4-05 ×2 + P3-06 rerun). Cost of the no-op: a full wasted pipeline run. (2) **Load-bearing logic inside an async RSC / next/headers boundary is hard to mutation-prove** — extract the invariant into a pure exported helper and test THAT (the RSC stays a thin caller). Generalize: security/data-integrity guards belong in pure functions, not inline in untestable boundaries.
- **Next concrete action**: P4-06 bulk operations via inline ship pipeline.

### 2026-06-14 — P3-05 page editor / block composer (reusable ship pipeline; opus 3-lens; ACCEPT-WITH-MINORS)
- **Changed**: lib/content/blocks/block-composer.ts (8 pure helpers), lib/content/blocks/block-editor-schemas.ts (BLOCK_EDITOR_SCHEMAS per registry type), app/(admin)/admin/pages/[id]/edit/page.tsx (editor), app/(admin)/admin/pages/page.tsx (Edit buttons wired), tests/unit/block-composer.test.ts (22 incl. L3 route-integration). Commit 606b6e6.
- **Verified**: tsc 0; 799 tests (+22 over 777); lint clean (1 pre-existing out-of-scope warning); no hex/px/any drift. Panel mutation-tested LIVE: forcing moveBlockUp no-op + leaking clientId in blocksToVersionPayload each killed tests → reverted. L3 e2e drives the REAL registerPagesRoutes Hono route + Zod createVersionBodySchema + createInMemoryContentStore + composer helpers (not in-test simulation). All 8 helpers + BLOCK_EDITOR_SCHEMAS have real consumers; Edit buttons router.push to the editor; autosave POSTs to /api/v2/admin/pages/:id/versions (requireAdmin).
- **Decisions**: D3 keystone upheld — per-block props editor is SchemaForm driven by block-editor-schemas (no per-block bespoke UI). Autosave append-per-change is in-spec (P3-04 /versions is append-only) but causes version-history spam → P3-05b routes the draft-overwrite question to P3-06. Closed P3-04a's edit-UI half (Edit button now wired); only deletePage remains on P3-04a.
- **Tooling**: extracted the ship pipeline into a REUSABLE parameterized workflow `.state/ship-packet.workflow.js` (untracked run tooling; opus reviewers baked in; packet/scout-targets/lenses via args). First reuse = P3-05; reuse for all remaining packets via Workflow scriptPath+args. (One cosmetic glitch: result.packetId defaulted to 'packet' though args.packet propagated — harmless.)
- **Next concrete action**: P3-06 preview + publish pipeline via the reusable pipeline.

### 2026-06-14 — P4-05 quantity inventory end-to-end (ship pipeline; opus 3-lens panel; ACCEPT-WITH-MINORS)
- **Changed**: db/queries/reservations.ts (atomic single-statement insertReservation via rawSql INSERT…SELECT…WHERE quantity_available>=qty RETURNING + getActiveReservationsCount), db/queries/products.ts (deriveQuantityAvailable), api/hono/routes/products.ts (admin PATCH dual-writes quantity when flag ON; /api/v2/products/:slug unchanged), app/(site)/collection/[slug]/page.tsx (PDP availability via deriveStockStatus+getActiveReservationsCount when flag ON), components/cart/add-to-cart-button.tsx (initialStatus seed), api/hono/routes/payments.ts (COMMENT-ONLY — stale "deferred to P4-05" corrected), tests/unit/inventory-v2.test.ts. Commit 7d1200b.
- **Verified**: tsc 0; 777 tests (inventory-v2 29/29); lint clean. Flag-OFF byte-identical on all 4 consumers (panel read the gates in source, not the report). TWO real mutation-proofs verified by the panel (it mutated source → confirmed fail → reverted): (1) insertReservation SQL WHERE quantity_available>=qty guard; (2) the AUTHORITATIVE oversell guard stock_status='available' in the payments.ts claim UPDATE.
- **Decisions**: insertReservation is an atomic PRE-CHECK only — it does NOT decrement quantity_available and reservations has NO unique constraint on product_id, so two concurrent qty=1 calls CAN both insert; the real one-winner serialization is the unchanged atomic stock_status UPDATE (+ rollback on rowcount mismatch → 409), runs in both flag states. Ran a dedicated HONESTY-REPAIR pass after the first panel round because the original docstrings/test overclaimed insertReservation as the oversell guard (would have caused a real oversell bug at P4-07 when stock_status is retired) and the "two concurrent claims" test was theater (mock-sequenced winner/loser). feeds-availability-mapping left as a P5 comment (out of scope). No migration run.
- **New failure modes**: (1) **The fable model alias (claude-fable-5) is UNAVAILABLE inside workflow subagents in this session** — `model:'fable'` throws "may not exist or you may not have access". Per user directive use `model:'opus'` for the adversarial reviewer (opus 4.8). (2) **Diff-baseline pollution**: verifiers/reviewers reason from `git diff HEAD`, but the working tree carries pre-existing uncommitted batched files (esp. tracked `.env.production.example`) → false "out-of-scope file modified" FAILs. Fix: PARK such files (`git stash push -- <path>`) so the baseline is clean, and scope reviewer prompts to an explicit in-scope file list. (3) The adversarial panel reliably catches money-path OVERCLAIMS the verifier passes — a single reviewer (now opus) and a 3-lens panel both earn their keep.
- **Next concrete action**: P3-05 page editor — block composer, via the ship pipeline (opus reviewer/panel).

### 2026-06-14 — P3-04 pages admin (ship pipeline; ACCEPT-WITH-MINORS)
- **Changed**: lib/ports/content-store.ts + lib/adapters/drizzle-content-store.ts + db/queries/content.ts (getPageById/updatePage/listPageVersions); lib/content/page-settings.schema.ts (new FormSchema); api/hono/routes/pages.ts (7 endpoints) + api/hono/app.ts (mount /admin/pages); app/(admin)/admin/pages/page.tsx (SchemaForm + version-history Sheet w/ restore); components/admin/nav-items.ts (Pages entry); tests/unit/pages-route.test.ts (15 tests). Commit a657a61.
- **Verified**: 763 tests; tsc+lint clean; 0 drift. Mutation-proven: reserved-slug rejected at BOTH store + route (409 SLUG_RESERVED); restore sets published_version_id to the CHOSEN prior version (not latest). Routes use the content-store port exclusively (no raw db). SEO/settings form is schema-DRIVEN via pageSettingsSchema.
- **Decisions**: nav-items.ts committed clean — the session-start batched labs nav entry (/admin/labs, non-shipping) was already absent from the worker's version, so no broken prod link shipped. Edit-page UI left as a placeholder (PATCH route + updatePage work; only the admin form is unwired) → P3-04a; deletePage absent from the port → P3-04a.
- **Next concrete action**: P4-05 quantity inventory end-to-end (atomic qty-aware claim, flag-gated FTT_FEATURE_INVENTORY_V2, flag-OFF byte-identical) via the ship pipeline.

### 2026-06-13 — P1-20 main leg merged to PRODUCTION (#G-P1 passed, user-authorized)
- **Changed**: merged origin/main (9 commits) into development (4d1ecf6); #35 merged sprint-abe→development (6f371a9); #36 merged development→main (**b1e9893 = prod**); sprint-abe synced (2a2e191). admin/orders/[id] island `order-status-editor.tsx` created.
- **Verified**: clean-worktree at the merge — `tsc` exit 0, **231 tests pass**; all functional CI green on #36 (build/test/lint/Vercel). main confirmed (git) to carry createRazorpayPaymentLink + guest-checkout proxy + status island + P1-08 const; xeno-slack-agent absent. Vercel prod deploy dpl_3bEkrnhbrJmh1sGgZfubXnZU2x6d (target=production) triggered.
- **Decisions**: 25-hunk merge resolved — money path took OURS (payment-link/guest/atomic completePaidOrder supersedes main's auth-gated Razorpay-Orders flow that blocked guests; main migration 0003 kept via journal); admin/orders/[id] kept main's SERVER component + ported P1-05 status editor as a client island; admin list took main's crash-fix; analytics storefront-only; drizzle journal kept both 0002_nullable-order-userid + 0003. #G-P1 prod merge done only after explicit user authorization ("just get it to main, dont ask me").
- **New failure modes**: `--ours` on a conflicted file takes the COMPLETE HEAD file (loses main's auto-merged additions) — safe only after diffing `git diff HEAD origin/main -- <file>` to confirm ours is a superset (did this for razorpay/payments/checkout/admin-orders). main's payments.ts was a DIFFERENT architecture (Razorpay Orders + requireAuth) not just hardening — blind "take theirs" would have re-broken guest checkout.
- **Next concrete action**: confirm prod deploy READY + live guest-checkout smoke; P1-15; then P2.

### 2026-06-13 — P1-19 + P1-20 slices + Xeno excision; dev PR opened (Wave 4 + ship-to-main)
- **Changed**: tests/unit/payments-route.test.ts + webhooks-route.test.ts (2e07c3d, P1-19, via background Workflow pipeline); emergency-fix slice (527bc02 — razorpay.ts/recipients.ts/proxy.ts/checkout confirmation+client); P1-test+migration slice (da16792); Xeno excision (a6676a9 — `git rm --cached` agent+test+example).
- **Verified**: clean git worktree at HEAD (true CI image) — `tsc --noEmit` exit 0, **231 tests pass** (41 files). P1-19 webhooks required a repair loop (WHERE-scoping was theater — proven by blanket-release mutation passing all tests; fixed with collectPrimitives AST assertion). payments-route ACCEPT-WITH-MINORS (P1-19a/b sub-tasks). Pushed sprint-abe; opened PR #35 (→development); closed #28.
- **Decisions**: (1) **HEAD didn't compile** — committed payments.ts (P1-08) imported createRazorpayPaymentLink/RAZORPAY_PAYMENT_LINK_HOLD_MINUTES/RAZORPAY_MIN_AMOUNT_PAISE from razorpay.ts, defined ONLY in the uncommitted working tree. The emergency-fix slice was build-critical, not optional. (2) **Xeno excised, not committed** — committed xeno-slack-agent.ts imported untracked lib/xeno/* which carries confidential data (real names, private channel ID, 3rd-party email); untracked the agent+test (kept on disk for the local socket bridge) rather than ship it. Completes P1-12's objective. (3) Flow is sprint-abe→development→main per user; development = staging.
- **New failure modes**: **Committed HEAD can be non-self-consistent when prior packets reference uncommitted working-tree symbols** — always verify in a CLEAN git worktree (untracked files mask dangling imports; `next-env.d.ts` is gitignored so a bare worktree needs it copied in for the ambient `*.png` decl, else false TS2307 on png imports). `recipients.ts` (business emails OK) and `clear-cart-on-confirmation.tsx` were also build-critical untracked files.
- **Next concrete action**: P1-20 main leg — `git merge origin/main` (9 behind; "take ours" on payments/razorpay/checkout/admin-orders), re-run ladder, PR development→main, #G-P1 deploy gate. Per-slice ship decision for the still-dirty admin-labs/saree-try-on/docs/tooling.

### 2026-06-13 — P1-10, P1-17, P1-18 executed via ship pipeline (Wave 3)
- **Changed**: lib/email/templates.ts+test (87c1305, P1-10); lib/seo/pdp-meta.ts+page+test (d18defd, P1-17); layout.tsx+lib/analytics/gtm.ts+test+package.json+.env.example (544443d, P1-18).
- **Verified**: 245/245 pass; tsc clean; fable-reviewer required 1 repair loop on P1-17 (truncation test theater + fabric/Heirloom saree edge) and P1-18 (admin layout scope creep + test theater). P1-10 ACCEPT-WITH-MINORS.
- **Decisions**: P1-10 — EmailOrder carries rupees (toEmailOrder divides paise by 100); templates.ts ×100 bridge is correct. Future packet (P1-10a) renames fields to *Paise and removes the bridge. P1-17 — fabric value normalized to strip trailing "saree" before building title suffix (prevents "Heirloom saree Saree"). P1-18 — GTM gate logic extracted to lib/analytics/gtm.ts so tests are not theater; admin routes excluded.
- **New failure modes**: P1-10 EmailOrder unit ambiguity — fields have no paise suffix; a future constructor passing raw paise would inflate prices 100×. Guard: lib/orders/complete-paid-order.ts:toEmailOrder is the only constructor; must stay as the single entry point.
- **Next concrete action**: P1-19 — route tests for payment-link money path (tests/unit/payments-route.test.ts + webhooks-route.test.ts; no prod code changes). Then P1-20 + #G-P1.

### 2026-06-13 — P1-07, P1-08, P1-11, P1-12 executed via ship pipeline (Wave 1)
- **Changed**: users.ts+queries/users.ts+test (2e86603, P1-07); rate-limit.ts+payments.ts+tests (0d1adc9, P1-08); order-access-token.ts+test (8f42fce, P1-11); xeno-slack-agent.ts+test+.gitignore (3e2aa5c, P1-12).
- **Verified**: 219/219 pass; tsc clean; fable-reviewer round required 2-loop repair on P1-07/P1-08/P1-12, 1-loop on P1-09.
- **Decisions**: P1-07 — claimCheckoutShell uses AND password_hash IS NULL predicate (prevents silent credential overwrite on concurrent claims). Spec escalation: unverified account claim design — anyone with the email can claim a checkout shell; accepted as-is pending principal decision. P1-08 — email normalized to lowercase throughout; pending cap uses paymentStatus+time-bound (not just status) to avoid locking out customers who abandoned checkouts. P1-11 — token format change intentionally breaks old permanent tokens (correct — they were the bug). P1-12 — WEAVE_X_CONTEXT moved to env var; test fixtures replaced real names with neutral placeholders.
- **New failure modes**: P1-08 catch block: if cleanup writes fail, original Razorpay error is masked (console.error runs after — route to P2-06). Drizzle AST inspection uses collectPrimitives WeakSet walker (circular references in PgTable columns).
- **Next concrete action**: Wave 2 — P1-14 alone (getSiteOrigin() helper, kill fromthetrunk.com fallback).

### 2026-06-13 — P1-06, P1-09, P1-13, P1-16 executed via ship pipeline
- **Changed**: db/queries/users.ts+payments.ts+schema+migration (7fdb2f9, P1-06); lib/http/on-uncaught-error.ts+app.ts+test (7ba0754, P1-09); webhooks.ts+test (2e4ce96, P1-13); json-ld-render.test.ts (f23448b, P1-16).
- **Verified**: 194/194 pass; tsc clean; fable-reviewer ACCEPT on all four (P1-09+P1-06 required repair loops for test theater and migration IF NOT EXISTS).
- **Decisions**: P1-06 — `orders.userId` must be nullable for guest orders with registered emails; schema change required beyond original packet spec. Drizzle migration uses IF NOT EXISTS to guard against out-of-band journal drift. P1-09 — handler extracted to lib/http/on-uncaught-error.ts to avoid importing heavy app.ts in tests.
- **New failure modes**: See Known Failure Modes section (JSON-LD </script>, orders.userId nullable, Drizzle migration journal drift).
- **Next concrete action**: Fan out P1-07 + P1-10/P1-11/P1-12/P1-14/P1-17 in parallel.

### 2026-06-13 — P1-02..P1-05 executed via ship pipeline
- **Changed**: `tsconfig.json`+`eslint.config.mjs` (94b063c, P1-02); `lib/email/send.ts`+test (67ff66d, P1-03); `lib/orders/complete-paid-order.ts`+test (331b3f5, P1-04); `api/hono/routes/admin-orders.ts`+`app/(admin)/admin/orders/[id]/page.tsx`+test (eceb7ee, P1-05).
- **Verified**: 174/174 pass; tsc clean across all packets; fable-reviewer ACCEPT on P1-03/P1-04 (after repair loops for test theater and wrong fixture); P1-05 ACCEPT after 2 repair loops (emailSent truth-value, dead scaffolding).
- **Decisions**: P1-04 — Drizzle mock WHERE predicate must be asserted via AST filter (`arg._and._ne`); test theater otherwise. P1-05 — `updateOrderStatus` already writes events internally; `addOrderEvent` call in route is a duplicate, removed.
- **New failure modes**: P1-05a TOCTOU (concurrent admin PATCHes both pass isStatusChange) — deferred sub-item. Note field `.max(500)` missing from admin PATCH schema — cosmetic, deferred.
- **Next concrete action**: Fan out P1-06 (blocks P1-07/08/19) + P1-09/10/11/12/13/14/16/17 in parallel.

### 2026-06-13 — P1-01 executed via ship pipeline
- **Changed**: `.gitignore` (a0be510) — root-scoped dirs `/output/`, `/outputs/`, `/tmp/`, `/.playwright-mcp/`; root-scoped file patterns for PNGs/JSONs; `/docs/finance/` added.
- **Verified**: `git check-ignore` exits 0 for all 9 paths; `public/apple-touch-icon.png` exits 1 (unaffected); porcelain count 71→62; verifier PASS, fable-reviewer ACCEPT-WITH-MINORS (2 minors resolved in repair loop, 1 false-claim in worker report with no code impact).
- **Decisions**: #G-GST resolved — GST-inclusive prices (P2-03 gate closed, P5 unblocked). `/docs/finance/` gitignored not committed (contains real financial data + personal filesystem paths).
- **New failure modes**: `git check-ignore` exit-0 check only verifies the path IS ignored, not the match depth — an any-depth pattern still exits 0. Always check for `/` prefix on root-scope intent; verifier must grep the entry, not just check-ignore.
- **Next concrete action**: Fan out P1-02..P1-14 in parallel (see dependency graph in P1-stabilize.md). P1-06 before P1-07. P1-04+P1-06 before P1-19.

### 2026-06-12 — Reviews + programme planning (no product code changed)
- **Changed**: created plans/ (README, master, P1–P6), .claude/agents/{repo-scout,implementation-worker,fable-reviewer}.md, verifier.md upgraded, .claude/skills/{ship,project-memory}, STATE.md. CLAUDE.md: session-protocol pointer added.
- **Verified**: full-codebase review (31-agent workflow, 43 confirmed findings, 3 refuted); external Shopify-report fact-check (5 agents — its JSON-LD bug claim REFUTED, its analytics/cron claims confirmed); npm test 166/166; lint clean.
- **Failed**: `tsc --noEmit` (16 errors, ftt-hr-gmail-workflow only).
- **Next concrete action**: /ship P1-01.

## Archive
(none yet)
